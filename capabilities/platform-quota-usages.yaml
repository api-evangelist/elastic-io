naftiko: 1.0.0-alpha2
info:
  label: elastic.io Platform REST API — Quota Usages
  description: 'elastic.io Platform REST API — Quota Usages. 2 operations. Lead operation: Elastic.io Get contract quota usage.
    Self-contained Naftiko capability covering one Elastic Io business surface.'
  tags:
  - Elastic Io
  - Quota Usages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ELASTIC_IO_API_KEY: ELASTIC_IO_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-quota-usages
    baseUri: https://api.elastic.io/v2
    description: elastic.io Platform REST API — Quota Usages business capability. Self-contained, no shared references.
    resources:
    - name: quota-usages-contracts-contract_id
      path: /quota-usages/contracts/{contract_id}
      operations:
      - name: getcontractquotausage
        method: GET
        description: Elastic.io Get contract quota usage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: quota-usages-workspaces-workspace_id
      path: /quota-usages/workspaces/{workspace_id}
      operations:
      - name: getworkspacequotausage
        method: GET
        description: Elastic.io Get workspace quota usage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ELASTIC_IO_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-quota-usages-rest
    port: 8080
    description: REST adapter for elastic.io Platform REST API — Quota Usages. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/quota-usages/contracts/{contract-id}
      name: quota-usages-contracts-contract-id
      description: REST surface for quota-usages-contracts-contract_id.
      operations:
      - method: GET
        name: getcontractquotausage
        description: Elastic.io Get contract quota usage
        call: platform-quota-usages.getcontractquotausage
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/quota-usages/workspaces/{workspace-id}
      name: quota-usages-workspaces-workspace-id
      description: REST surface for quota-usages-workspaces-workspace_id.
      operations:
      - method: GET
        name: getworkspacequotausage
        description: Elastic.io Get workspace quota usage
        call: platform-quota-usages.getworkspacequotausage
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-quota-usages-mcp
    port: 9090
    transport: http
    description: MCP adapter for elastic.io Platform REST API — Quota Usages. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: elastic-io-get-contract-quota-usage
      description: Elastic.io Get contract quota usage
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-quota-usages.getcontractquotausage
      outputParameters:
      - type: object
        mapping: $.
    - name: elastic-io-get-workspace-quota-usage
      description: Elastic.io Get workspace quota usage
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-quota-usages.getworkspacequotausage
      outputParameters:
      - type: object
        mapping: $.
